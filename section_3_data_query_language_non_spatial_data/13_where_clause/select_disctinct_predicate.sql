-- WHERE Clause is used to limit our query
-- Make sure to notice all reserved keyword in SQL
-- LIKE predicate to find all svy_result column THAT HAS character 'ACT' = '%ACT%'

SELECT DISTINCT svy_user FROM raptor_surveys;